
#include "KeySpec.h"

// Include KeySpecInitialize.h here.
// It defines the KeySpec::InitializeStaticData() method, which is autogenerated by 
// the KeySpecXMLCompiler.py script.
#include ".\resources\KeySpecInitialize.h"														// CR.32662; added.


KeySpec::KeySpec()
{
	//#include ".\resources\KeySpecInitialize.h"													// CR.32662; commented.
	InitializeStaticData();																				// CR.32662; added.
}

KeySpec::~KeySpec()
{
	;
}

void KeySpec::Product::Initialize()
{
	for (unsigned int i=0; i<data.size(); ++i)
	{
		lookup_int[data[i].id]=i;
		lookup_string[data[i].name]=i;
	}
}

void KeySpec::ProductList::Initialize()
{
	for (unsigned int i=0; i<data.size(); ++i)
	{
		lookup_int[data[i].id]=i;
		lookup_string[data[i].name]=i;
	}
}

void KeySpec::HeaderList::Initialize()
{
	for (unsigned int i=0; i<data.size(); ++i)
	{
		lookup_int[data[i].id]=i;
		lookup_string[data[i].name]=i;
	}
}